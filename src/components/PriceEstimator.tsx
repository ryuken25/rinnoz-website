"use client";
import { useMemo, useState } from 'react'; import { pricing, StyleKey } from '@/content/pricing';
export function PriceEstimator(){ const [style,setStyle]=useState<StyleKey>('Chibi'); const [type,setType]=useState<string>(pricing.Chibi[0].type); const [chars,setChars]=useState(1); const [bg,setBg]=useState('None/simple'); const current=pricing[style].find(p=>p.type===type)||pricing[style][0];
 const estimate=useMemo(()=>({ usd: current.usd*chars+(bg==='Complex'?10:0), idr: current.idr*chars+(bg==='Complex'?50000:0)}),[current,chars,bg]);
 return <div className="card p-6"><p className="eyebrow">Rough estimate helper</p><h3 className="mt-2 font-display text-3xl">Plan your order</h3><p className="helper mt-2">Only a rough guide. Final price is confirmed by RinnOZ after reviewing complexity.</p><div className="mt-5 grid gap-4 sm:grid-cols-2"><label>Style<select value={style} onChange={e=>{const s=e.target.value as StyleKey;setStyle(s);setType(pricing[s][0].type)}}><option>Chibi</option><option>Anime</option></select></label><label>Type<select value={type} onChange={e=>setType(e.target.value)}>{pricing[style].map(p=><option key={p.type}>{p.type}</option>)}</select></label><label>Character count<input type="number" min={1} max={10} value={chars} onChange={e=>setChars(Math.max(1,Number(e.target.value)||1))}/></label><label>Background<select value={bg} onChange={e=>setBg(e.target.value)}><option>None/simple</option><option>Complex</option></select></label></div><div className="mt-5 rounded-3xl border border-lavender/25 bg-lavender/10 p-4"><p className="text-sm text-cream/65">Estimated base</p><p className="font-display text-4xl">${estimate.usd} / IDR {Math.round(estimate.idr/1000)}k+</p><p className="mt-1 text-sm text-cream/60">Commercial use and rush deadlines must be discussed first.</p></div></div> }
