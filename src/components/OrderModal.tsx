"use client";
import Image from 'next/image';
import { AnimatePresence, motion } from 'framer-motion';
import { useCallback, useEffect, useMemo, useRef, useState } from 'react';
import { pricing, type StyleKey } from '@/content/pricing';
import { socialArtworks } from '@/content/socialArtworks';
import { discordUrl, instagramUrl, xUrl } from '@/content/socials';

type Lang='en'|'id'; type Values=Record<string,string>;
const initial:Values={name:'',email:'',contactPlatform:'Instagram',contactHandle:'',paymentMethod:'Mandiri',commissionStyle:'Chibi',type:'Headshot',characterCount:'1',background:'None/simple',usage:'Personal',deadline:'Flexible',vibe:'',description:'',lore:'',design:'',references:'',notes:'',tos:''};
const labels={en:['Contact','Commission','References','Review'],id:['Kontak','Komisi','Referensi','Review']};
function summary(v:Values,lang:Lang){return `RinnOZ Commission Order
Submitted: ${new Date().toLocaleString()}
Source: ${location.href}
Language: ${lang.toUpperCase()}

CLIENT
Name / handle: ${v.name}
Email: ${v.email}
Preferred contact: ${v.contactPlatform}
Contact username/link: ${v.contactHandle}

COMMISSION
Style: ${v.commissionStyle}
Type: ${v.type}
Characters: ${v.characterCount}
Background: ${v.background}
Usage: ${v.usage}
Deadline: ${v.deadline}
Vibe / mood: ${v.vibe}
Payment method: ${v.paymentMethod}

DETAILS
Description: ${v.description}
Personality / lore / story: ${v.lore}
Design enhancements / accessories: ${v.design}
References: ${v.references}
Additional notes: ${v.notes}

TERMS
Client agreed to RinnOZ Terms of Service: ${v.tos?'yes':'no'}`}
function Field({label,children,help}:{label:string;children:React.ReactNode;help?:string}){return <label className="space-y-2"><span>{label}</span>{children}{help&&<small className="helper block">{help}</small>}</label>}
function estimate(v:Values){const style=(v.commissionStyle==='Anime'?'Anime':'Chibi') as StyleKey; const item=pricing[style].find(p=>p.type===v.type)||pricing[style][0]; const chars=Math.max(1,Number(v.characterCount)||1); const extra=v.background==='Complex'?{usd:10,idr:50000}:{usd:0,idr:0}; return {usd:item.usd*chars+extra.usd,idr:item.idr*chars+extra.idr,label:`${item.usdLabel} / ${item.idrLabel}`}}
export function OrderModal(){const [open,setOpen]=useState(false);const [dirty,setDirty]=useState(false);const [step,setStep]=useState(0);const [lang,setLang]=useState<Lang>('en');const [v,setV]=useState<Values>(initial);const [status,setStatus]=useState('');const [mailto,setMailto]=useState('');const closeRef=useRef<HTMLButtonElement|null>(null);const est=estimate(v);const sum=useMemo(()=>open?summary(v,lang):'', [v,lang,open]);const sample=socialArtworks.filter(a=>v.commissionStyle==='Chibi'?a.tags.includes('Chibi'):a.tags.includes('Anime')).slice(0,4);
 function set(k:string,val:string){setDirty(true);setV(x=>({...x,[k]:val}))} function input(k:string){return {value:v[k]||'',onChange:(e:React.ChangeEvent<HTMLInputElement|HTMLTextAreaElement|HTMLSelectElement>)=>set(k,e.target.value)}}
 const requestClose=useCallback(()=>{ if(dirty&&!confirm('Close the order form? Your draft is not submitted yet.')) return; setOpen(false); if(location.hash==='#order') history.replaceState(null,'',location.pathname+location.search); },[dirty]);
 useEffect(()=>{function click(e:MouseEvent){const a=(e.target as HTMLElement).closest('a[href="#order"]') as HTMLAnchorElement|null; if(a){e.preventDefault(); document.getElementById('order')?.scrollIntoView({behavior:'smooth',block:'center'}); setOpen(true); history.replaceState(null,'','#order');}} document.addEventListener('click',click); if(location.hash==='#order') setTimeout(()=>setOpen(true),100); function hash(){if(location.hash==='#order') setOpen(true)} window.addEventListener('hashchange',hash); return()=>{document.removeEventListener('click',click); window.removeEventListener('hashchange',hash)}},[]);
 useEffect(()=>{if(!open)return;const old=document.body.style.overflow;document.body.style.overflow='hidden';setTimeout(()=>closeRef.current?.focus(),80);function key(e:KeyboardEvent){if(e.key==='Escape')requestClose()}window.addEventListener('keydown',key);return()=>{document.body.style.overflow=old;window.removeEventListener('keydown',key)}},[open,requestClose]);
 async function submit(){setStatus('Sending order...');setMailto('');try{const r=await fetch('/api/order',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...v,language:lang,source:location.href})});const j=await r.json();setStatus(j.ok?'Order sent successfully. Keep a copy of your summary too.':`Failed: ${j.error}`); if(j.ok)setDirty(false); if(j.mailto)setMailto(j.mailto)}catch{setStatus('Failed to send. Please use manual email.');setMailto(`mailto:takayuki.rinnozuki@gmail.com?subject=${encodeURIComponent(`[RinnOZ Commission] New order from ${v.name} — ${v.commissionStyle}/${v.type}`)}&body=${encodeURIComponent(sum)}`)}}
 return <AnimatePresence>{open&&<motion.div className="fixed inset-0 z-[70] bg-ink/54 p-0 backdrop-blur-xl md:p-5" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onMouseDown={requestClose} role="dialog" aria-modal="true" aria-label="RinnOZ commission order studio"><motion.div initial={{opacity:0,scale:.96,y:28}} animate={{opacity:1,scale:1,y:0}} exit={{opacity:0,scale:.96,y:20}} transition={{type:'spring',stiffness:180,damping:22}} onMouseDown={e=>e.stopPropagation()} className="mx-auto flex h-dvh max-w-7xl flex-col overflow-hidden border-white/15 bg-midnight shadow-atelier md:h-[92vh] md:rounded-[2.2rem] md:border"><div className="flex items-center justify-between border-b border-white/10 p-4"><div><p className="eyebrow">Commission order studio</p><h2 className="font-display text-2xl">Start your commission</h2></div><button ref={closeRef} className="btn btn-ghost" onClick={requestClose}>Close</button></div><div className="grid min-h-0 flex-1 overflow-auto lg:grid-cols-[390px_1fr]"><aside className="border-b border-white/10 bg-white/[.035] p-5 lg:border-b-0 lg:border-r"><p className="eyebrow">Live preview</p><h3 className="mt-2 font-display text-4xl">Let’s plan your artwork</h3><div className="mt-5 rounded-3xl border border-lavender/25 bg-lavender/10 p-4"><p className="text-sm text-cream/62">Selected style</p><p className="font-display text-3xl">{v.commissionStyle} / {v.type}</p><p className="mt-1 text-sm text-cream/60">{v.characterCount} character(s) • {v.background} background</p></div><div className="mt-4 rounded-3xl border border-blush/25 bg-blush/10 p-4"><p className="text-sm text-cream/62">Rough estimate</p><p className="font-display text-4xl">${est.usd}+ / IDR {Math.round(est.idr/1000)}k+</p><p className="text-xs text-cream/55">Final price confirmed by RinnOZ after review.</p></div><div className="mt-4 rounded-3xl border border-white/10 bg-white/5 p-4"><p className="font-black text-lavender">Timeline</p><p className="text-sm text-cream/65">Typical timeline: 5–30 days. Rush request may add fee.</p></div><div className="mt-4 grid grid-cols-2 gap-2">{sample.map(s=><Image key={s.id} src={s.imageUrl} alt={s.title} width={s.width} height={s.height} className="aspect-square rounded-2xl object-cover"/> )}</div><p className="mt-4 text-sm text-cream/55">Tip: prepare Google Drive, Toyhouse, Pinterest board, or direct image links before submitting.</p></aside><section className="p-5"><div className="mb-5 flex flex-wrap items-center justify-between gap-3"><div className="flex gap-2">{labels[lang].map((s,i)=><button key={s} onClick={()=>setStep(i)} className={`step-dot ${i===step?'active':''}`} aria-label={s}>{i+1}</button>)}</div><div className="flex rounded-full border border-white/10 bg-white/5 p-1"><button className={`rounded-full px-4 py-2 font-black ${lang==='en'?'bg-lavender text-ink':''}`} onClick={()=>setLang('en')}>EN</button><button className={`rounded-full px-4 py-2 font-black ${lang==='id'?'bg-lavender text-ink':''}`} onClick={()=>setLang('id')}>ID</button></div></div>{step===0&&<div className="grid gap-4 md:grid-cols-2"><Field label="Name / social handle"><input required {...input('name')}/></Field><Field label="Email"><input required type="email" {...input('email')}/></Field><Field label="Preferred contact"><select {...input('contactPlatform')}><option>Instagram</option><option>X</option><option>Discord</option><option>Email</option><option>Other</option></select></Field><Field label="Contact username/link"><input {...input('contactHandle')}/></Field></div>}{step===1&&<div className="grid gap-4 md:grid-cols-2"><Field label="Payment"><select {...input('paymentMethod')}><option>Mandiri</option><option>Dana</option><option>PayPal</option></select></Field><Field label="Style"><select {...input('commissionStyle')}><option>Chibi</option><option>Anime</option><option>Other</option><option>Discuss first</option></select></Field><Field label="Type"><select {...input('type')}><option>Headshot</option><option>Bust Up</option><option>Half Body</option><option>Full Body</option><option>Emote</option><option>Character Sheet</option><option>Other</option></select></Field><Field label="Characters"><input type="number" min="1" {...input('characterCount')}/></Field><Field label="Background"><select {...input('background')}><option>None/simple</option><option>Complex</option></select></Field><Field label="Usage"><select {...input('usage')}><option>Personal</option><option>Commercial discussion needed</option></select></Field><Field label="Deadline"><select {...input('deadline')}><option>Flexible</option><option>Specific date</option><option>Rush</option></select></Field><Field label="Vibe / mood"><input {...input('vibe')}/></Field></div>}{step===2&&<div className="grid gap-4 md:grid-cols-2"><Field label="Description"><textarea required {...input('description')}/></Field><Field label="Personality / lore / story"><textarea {...input('lore')}/></Field><Field label="Design / accessories"><textarea {...input('design')}/></Field><Field label="Reference links" help="Google Drive, Imgur, Toyhouse, Pinterest, direct links, or DM refs."><textarea required {...input('references')}/></Field><Field label="Additional notes"><textarea {...input('notes')}/></Field></div>}{step===3&&<div className="grid gap-5 xl:grid-cols-[1fr_330px]"><pre className="max-h-[54vh] whitespace-pre-wrap rounded-3xl border border-white/10 bg-ink/55 p-5 text-sm leading-6 text-cream/78">{sum}</pre><div className="space-y-3"><label className="flex gap-3 rounded-3xl border border-white/10 bg-white/5 p-4 text-cream/80"><input className="mt-1 w-auto" type="checkbox" checked={v.tos==='yes'} onChange={e=>set('tos',e.target.checked?'yes':'')}/>I agree to RinnOZ Terms of Service.</label><button className="btn btn-primary w-full" onClick={submit}>Submit Order</button><button className="btn btn-ghost w-full" onClick={()=>navigator.clipboard.writeText(sum)}>Copy Summary</button><a className="btn btn-ghost w-full" href={mailto||`mailto:takayuki.rinnozuki@gmail.com?subject=${encodeURIComponent(`[RinnOZ Commission] New order from ${v.name} — ${v.commissionStyle}/${v.type}`)}&body=${encodeURIComponent(sum)}`}>Email Manually</a><div className="grid grid-cols-3 gap-2"><a className="btn btn-ghost" href={instagramUrl} target="_blank">IG</a><a className="btn btn-ghost" href={discordUrl} target="_blank">Discord</a><a className="btn btn-ghost" href={xUrl} target="_blank">X</a></div>{status&&<p className="rounded-3xl border border-white/10 bg-white/5 p-4 text-cream/75">{status}</p>}</div></div>}<div className="mt-7 flex justify-between"><button className="btn btn-ghost" disabled={step===0} onClick={()=>setStep(s=>Math.max(0,s-1))}>← Back</button>{step<3&&<button className="btn btn-primary" onClick={()=>setStep(s=>Math.min(3,s+1))}>Next →</button>}</div></section></div></motion.div></motion.div>}</AnimatePresence>}
